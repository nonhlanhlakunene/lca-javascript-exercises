const colors = [];

colors.push('Red', 'Orange', 'Green'); // push adds colors 

const numbers = [1, 4, 8, 7, 5];


colors.pop(); //pop removes last array
colors.unshift("Purple"); // unshift adds new color to beginning


console.log("The length of the colors array is", colors.length);
console.log("The length of the numbers array is", numbers.length);
